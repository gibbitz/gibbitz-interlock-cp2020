import { systemLog } from "../../../utils"
// TODO: get relevant stat from Item props
// get actor
// display item in popup (like the system default)
// Active Effects?
export const displayItemRoll = (...args) => {
  systemLog('OTHER ITEM DISPLAY | ', ...args)
  // if there is an active Effect present a button to use it
}