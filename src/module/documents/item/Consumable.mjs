import { Outfit } from './Outfit.mjs';

/**
 * Extend the Outfit with Consumable specific methods.
 * @extends {Outfit}
 */
export class Consumable extends Outfit {
  _use (quantity) {
    debugger
  }
}