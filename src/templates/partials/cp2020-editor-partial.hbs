<span class="field-box {{#if class}}{{class}}{{/if}}">
  <span class="field-box--inner">
    <label>
      {{editor
      item
      target=target
      engine="prosemirror"
      }}
      <small>{{localize label}}</small>
    </label>
  </span>
</span>