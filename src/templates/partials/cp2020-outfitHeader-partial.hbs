{{systemLog ' HEADER | ' this}}
<header class="sheet-header {{#if class}}{{class}}{{/if}}">
  <div class="header-fields">
    {{localize label}}
    <h1 class="charname">{{name}}</h1>
  </div>
</header>